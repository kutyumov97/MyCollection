@model Item
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="nav-item">
                Item @Model.Name
            </h2>
        </div>
        <div class="col-12 text-end">
            <a asp-controller="Item" asp-action="Index" asp-route-id="@GlobalVariables.CollectionId" class="btn btn-primary" style="width:200px">
                <i class="bi bi-reply"></i>&nbsp;
                Back to item list 
            </a>
        </div>
    </div>
    <br /><br />

<table class="table table-hover table-striped" style="width:100%">
    <thead>
        <tr>
            <th width="20%">
                Item name - @Model.Name
            </th>
            <th width="60%">
                Tags - @Model.Tags
            </th>
        </tr>
    </thead>
    <tbody>
        @*@foreach(var obj in Model)
        {
            <tr>
                <td width="20%">
                    @obj.Name
                </td>
                <td width="60%">
                    @obj.Tags
                </td>
                <td align="left">
                    <div class="w-100 btn-group" role="group">
                        <a asp-controller="Item" asp-action="Details" asp-route-id=@obj.Id class="btn btn-primary mx-2">
                            <i class="bi bi-list-ul"></i>
                        </a>
                    </div>
                </td>
                @if (GlobalVariables.UserId == obj.AppUserId)
                {
                    <td align="left">
                        <div class="w-100 btn-group" role="group">
                            <a asp-controller="Item" asp-action="Edit" asp-route-id=@obj.Id class="btn btn-primary mx-2">
                                <i class="bi bi-pencil"></i>
                            </a>
                        </div>
                    </td>
                    <td align="left">
                        <div class="w-100 btn-group" role="group">
                            <a asp-controller="Item" asp-action="Del" asp-route-id=@obj.Id class="btn btn-danger mx-2">
                                <i class="bi bi-trash3"></i>
                            </a>
                        </div>
                    </td>
                }
                else 
                {
                    <td align="left">
                        <div class="w-100 btn-group" role="group">
                            <a class="btn btn-secondary mx-2">
                                <i class="bi bi-lock"></i>
                            </a>
                        </div>
                    </td>
                    <td align="left">
                        <div class="w-100 btn-group" role="group">
                            <a class="btn btn-secondary mx-2">
                                <i class="bi bi-lock"></i>
                            </a>
                        </div>
                    </td>
                }
            </tr>
        }*@
    </tbody>
</table>
</div>